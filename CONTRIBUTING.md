# Contributing

1. Install Rust 1.88 or newer.
2. Run `cargo fmt --all -- --check`.
3. Run `cargo clippy --all-targets --all-features -- -D warnings`.
4. Run `cargo test --all`.
5. Keep all destructive operations preview-first and covered by tests.

